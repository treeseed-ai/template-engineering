---
id: engineering-parent-root
agent: architect
kind: workday
fixture: src/lib/normalize-release-channel.ts
trigger:
  workflowKind: engineering-test-first
  objective: ship-the-first-guided-change
  approvedDecision: normalize-release-channel-inputs
  exactBaseRef: fixture-head
  requireRevisionCycle: true
expect:
  requiredAgents:
    - architect
    - researcher
    - tester
    - engineer
    - reviewer
    - technical-writer
    - releaser
    - reporter
  requiredSequence:
    - approved-decision-and-readiness
    - exact-ref-worktree
    - research-and-architecture-when-required
    - failing-test
    - integration-ref-captured
    - implementation-without-test-mutation
    - verification
    - review-and-revision
    - documentation
    - release-readiness
    - operations-runner-handoff
  assertions:
    - acting is rejected without an approved decision, readiness, and accepted capacity-plan provenance
    - every code assignment uses an isolated worktree created from the recorded exact base ref
    - the tester produces a failing regression test and verification receipt before implementation starts
    - the engineer consumes the recorded test integration ref and does not modify test files
    - review failure creates revision work instead of advancing release readiness
    - agents use TreeSeed workflow operations and never push, merge, tag, deploy, or mutate provider infrastructure directly
    - the final report includes assignments, mode runs, providers, TreeDX artifacts, verification, usage, and settlement references
---

Run the real decision, capacity, provider, assignment, AgentKernel, worktree, verification, review, documentation, operations-runner, usage, and settlement lifecycle from the configured parent project root while preserving TreeSeed content boundaries.
